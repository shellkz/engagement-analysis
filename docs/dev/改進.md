1. 10000 threads[done]
3. Time-based split vs Random splilt[done]
2. 基本特徵的額外實驗(category)
4. Binary-classication vs Value Regression
5. 類別不平衡：
調整分類閾值（Threshold Tuning）、對抗過採樣（SMOTE）或改用樹狀模型 Random Forest 或 XGBoost


1. 測試看看label移除mention避免過度樂觀(因為feature很多mention)
發現移除後ROC-AUC : 0.856 -> 0.860
反而上升，降低過度樂觀的懷疑

2. early時間窗口跟roc auc關係
看看每天的熱門比例的比例如果不均(畢竟有些天會熱門多)，可能某些天的特徵影響更多

3. 60分鐘時平均有幾篇 post？占整體太多可能會太樂觀(初期幾乎等於最終)

